import React from 'react';
import Hero from './Hero';
import './home.scss';

const Home = (props) => (
  <div>
    <Hero />
    <div className="home">
      HELLO WHERE AM I
    </div>
  </div>
);

export default Home;
