import React, { useState } from 'react';
import {
  Box,
} from '@chakra-ui/react';

const Nav = ({}) => {
  const [test, setTest] = useState('hello');

  return (
    <Box>
      <Box>
        hello
      </Box>
    </Box>
  );
};

export default Nav;
